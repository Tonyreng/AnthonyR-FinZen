export type LoginFormInputs = {
    email: string;
    password: string;
    rememberMe: boolean;
};

export type SmallUser = {
    id: number;
    fullName: string;
    email: string;
    currency: string;
    creaetedAt: string;
    updatedAt: string;
};
